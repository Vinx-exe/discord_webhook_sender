do{
var sign = prompt("Press \"Ok\" to continue to site");
}while(sign == null || input == "");
while(sign == "" || input =="");
// Make sure the word/letter is the same as above
if (sign.toString() == "Ok") {
}
else {
window.location.reload()
}